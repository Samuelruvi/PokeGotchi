<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PokeGotchi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PokeGotchi" parent="Base.Theme.PokeGotchi" />

    <!-- Material3 hace el FloatingActionButton un cuadrado con esquinas redondeadas por
         defecto - el usuario pidio explicitamente "el tipico circulito", asi que se fuerza
         la forma a un circulo perfecto (radio de esquina = 50%). -->
    <style name="ShapeAppearanceOverlay.CircleFab" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>